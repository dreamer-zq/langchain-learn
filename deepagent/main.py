from memory import example_long_term_memory, example_short_term_memory
from human_in_loop import example_human_in_the_loop
from sub_agent import example_multi_agent
from middleware import example_demo_login
import asyncio


if __name__ == "__main__":
    # example_long_term_memory()
    # example_short_term_memory()
    # example_human_in_the_loop()
    # asyncio.run(example_multi_agent())
    example_demo_login()
