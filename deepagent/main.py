from memory import process_long_term_memory, process_short_term_memory
from human_in_loop import human_in_the_loop
from sub_agent import example_multi_agent
from middleware import demo_login_example
import asyncio


if __name__ == "__main__":
    # process_long_term_memory()
    # process_short_term_memory()
    # human_in_the_loop()
    # asyncio.run(example_multi_agent())
    demo_login_example()
